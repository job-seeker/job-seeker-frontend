import jobActions from '../action/job-actions.js';
import superagent from 'superagent';


describe('Job Actions', () => {


  test('Job_FETCH should return a job', () => {
    
  });

  test('Job_CREATE should create a job', () => {
    
  });

  test('Job_UPDATE should edit a job', () => {
    
  });

});